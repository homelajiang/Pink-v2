<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/mp.list.itemHeight"
    android:background="@color/mp.list.background">

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/mp.divider.line"
        android:background="?attr/selectableItemBackground" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:drawablePadding="5dp"
        android:layout_marginLeft="@dimen/mp.margin.normal"
        android:layout_marginStart="@dimen/mp.margin.normal"
        android:drawableLeft="@drawable/ic_play_circle_outline_black_24dp"
        android:drawableStart="@drawable/ic_play_circle_outline_black_24dp"
        android:gravity="center_vertical"
        android:text="@string/pink_play_all" />

    <RadioGroup
        android:id="@+id/radio_group"
        android:layout_width="wrap_content"
        android:layout_height="?android:attr/actionBarSize"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_gravity="end"
        android:divider="@drawable/common_divider_large_horizontal"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:showDividers="beginning|middle|end"
        tools:showIn="@layout/fragment_main">

        <RadioButton
            android:id="@+id/radio_btn_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:button="@drawable/selector_music_item_view"
            tools:button="@drawable/selector_music_item_view" />

        <RadioButton
            android:id="@+id/radio_btn_list"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:button="@drawable/selector_music_item_list"
            android:checked="true"
            tools:button="@drawable/selector_music_item_list" />
    </RadioGroup>

</RelativeLayout>